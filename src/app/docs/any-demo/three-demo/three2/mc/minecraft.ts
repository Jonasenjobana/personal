export class mcbox {
    cellsize = 32; // 32个单位几为1
    position: [number, number, number]
    constructor() {

    }
    getCell(x, y, z) {
        const cellsize = this.cellsize;
        const startX = x * cellsize;
        const startY = y * cellsize;
        const startZ = z * cellsize;

    }
    getVoxel() {
        
    }
}